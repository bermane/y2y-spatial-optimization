# Alberta Y2Y Prioritization — Analysis Spec (Mirror of Y2Y-Wide v0.13)

**Status:** v0.4.6 — **Gates AB-0a, AB-0, AB-1, AB-2 MEASURED 2026-09-03 (results_log R1–R5). D-AB10 ratified: applied band g = 2%, per-scenario frequency surfaces first-class; nesting re-evaluation at 2% PENDING (07/08 re-run). Then 09 freeze (commit pipeline modules first) → 10 ensemble.** Headlines: AB 32.9% locked (30% referent infeasible; effective 44.7%); H-AB1 refuted (mammals flips inexpressible, S5 stays inexpressible); m_soc scale-stable (t 0.322) but PRE-SATISFIED at 71.4% banked (floor ≈0.765 → S4 needs θ ≤2×); 27/40 EFGs. Review round 1 applied (Ethan's flag rulings + parent re-pin). Data acquisition list open (§8); D-AB5 anchor corrected (X and infeasibility check land at AB-0a); D-AB6 open.
**Scope:** Applied decision-support run for Y2Y Alberta program (per Tim Burkhart scoping answers, 2026-09) + scale-transfer methods test: how the frozen Y2Y-wide methodology behaves at subregional extent on a substantially more disturbed landscape. Separate analysis folder; consumes the Y2Y-wide pipeline as a library. NOT a paper-1 component (parent §14 fences remain; anything here that feeds a paper is applied-paper material).
**Mirror declaration:** Mirrors `frequency_ensemble_study_plan.md` **v0.14.1** (re-pinned 2026-09-03; was v0.13). What is mirrored is the frozen **decision procedure** — §2.5 rules R1–R4 with constants θ=5×, λ=0.10, a_min=0.5%, t_min=0.15; block accounting in two-regime swing currency; scenario-construction rules (§3.1 structure); numerics standards (§2.6); estimand and aggregation (§4); manifest discipline (§9); and the parent v0.14 rulings — **dual-semantics reporting** (aggregate 5% band = the Claim-A estimand; per-block-floor guarded variant, capture_b ≥ 0.95·anchor_b, = the applied headline: "no value theme left >5% behind"), **D quoted from MGA only** (E12 ruling; f is instrument-robust per the parent bracket, no MAA here), the **meaningful-target-window test** on every derived target (E10: a target below the co-capture + lock-in floor is a decoration; window priced by the Lorenz curve), and the **audit-space vs lock-conditional disclosure** (M3.6). Design points are "formulations" (M4.17). NOT mirrored: the Y2Y-extent measurements. Leverage values, class assignments, θ-tails, and the frozen (w,t) vectors in parent §2.5/§3.1 are properties of the Y2Y extent and are expected to differ here. Alberta derives its own characterization table and `scenarios_ab_v1.json` by the identical protocol. Upstream spec amendments propagate by re-pin: bump the mirror version here, log the delta, re-run affected stages.
**Companion documents:** parent spec v0.14.1; parent reporting spec `analyses/y2y/spec/director_package_spec.md` (v1.1 + v1.3 conventions section — presentation grammar mirrored, aggregation constants NOT, per D-AB7); `gate_ab0a_reportback.md` (future); Tim Burkhart scoping thread (2026-09).

## Changelog
- v0.4.6 — Gate AB-2 measured (R5): H-AB4 refuted in the opposite direction (D 0.9999 / C 0 at both levels; 5% frequent tier 2 / 0 km²; guardrails inert; f(g) at A: 2% core 876 / frequent 1,488 km²); nesting test vacuous at 5% (M9). **Ethan ratified (a)+(b)+(c) → D-AB10: applied band g = 2% for every frequency product (ensemble F and per-formulation f); nesting re-evaluated at 2%; primary level follows.** Deliverable definition made explicit: the value-forward scenarios' own frequency surfaces are first-class products beside the ensemble F. 07/08 patched (2% at both levels; nesting by band), 10 patched (2% sweeps per formulation), 09 gains `applied_band_g`.
- v0.4.5 — AB-0 arms RUN (04, ~1 s each): a0 m_soc 0.867 / a1 0.744 (pre-satisfied, zero pull → the E10 co-capture floor) / a4 objective-equivalent to a0 (Δ = the 0.2 constant) / a5 (θ 2×, 0.772) binds at the kink. The S4 ladder's pre-registered stop fired on the first 05 run because the rule named a0 (capture WITH carbon pull) as the floor; corrected to a1 (M5.7) — D3 θ 2× stands, certified by a5. Notebooks 06–08 (AB-1/AB-2) built (M7); §12 = 12 notebooks.
- v0.4.4 — D-AB3 private-lands ESTIMATE equation formalised (Ethan: "run 03 now, with the private lands estimate equation"); open layers acquired (`data/provenance.json`; GVI unzipped locally, 3.5 GB, gitignored); `03_ab0a_tenure_aoi` built. Data facts absorbed: the SRP zones file's **Nature First polygons total 440 km²** = the two Proposed Conservation Areas (352 + 87); the press 2,200–3,200 km² figures include Willmore/Kakwa — AOI-2 uses the shapefile. Crown Land Reservations (open, active) + PLUZ serve as the crown indicator inside the White Area in lieu of dispositions. D-AB6 tabled → `dist_to_pa_km.tif` prepared for the post-hoc reading.
- v0.4.3 — Gate AB-0a rulings absorbed (methods_log M4): D-AB9 (biodiversity block whole; mammals' inert weight disclosed); D2 mirror (S0–S3 m_soc t = 0.322 though pre-satisfied); D3 → **S4 ladder** pre-registered (§12; θ 2× → 1.5× → 1.2× against the measured a0 floor, pilot-certified); D4 corridors literal-rule; D-AB6 TABLED (proximity read post hoc); **D-AB5 v2 two-level budget axis** with the frozen nesting rule (N ≥ 0.80). AB-0 notebooks built: `04_ab0_arms` (R) + `05_ab0_scenarios` (py); §12 renumbered (11 notebooks).
- v0.4.2 — Parent REPORTING spec re-read (`director_package_spec.md`, new "Presentation conventions borrowed from precedent (v1.3)" section; its status line still reads v1.1). Mirrored the scale-independent conventions into §6: tier-achievement figure; % area on every scenario map + inset F histogram; Act-1 (a)/(b) pair re-cast for D-AB2 (masked additions view / locked-estate context view); anchor agreement matrix; T-D4-analog by Alberta Natural Region/Subregion AND by tenure class; E17 precedent line. §7 C4 adopts the parent's binding "alignment, not assignment" language rule for the AOI overlays (AOIs not locked in ⇒ independent convergence). D-AB7 gains the AB scale sentence (display at/near 1 km; Currie's 100 km² hexbins are precedent for coarsening, not a target). D-AB8 ask ON HOLD (Ethan 2026-09-03).
- v0.4.1 — BUILD STARTED (Ethan "start", 2026-09-03). D-AB7 registered: presentation aggregation (hexes, cluster minimum size, opportunity framing) is set at the AB scale, not inherited from the director package — the one designed divergence from the parent; procedure inherited, constants re-set with disclosure. §12 build record added (10 notebooks; AB-0a split into extent/stack → audit → tenure/AOI). Engine design: AB hand-off stack = parent stack masked on the same grid (`input_data/aligned_stack_ab/`), so every parent module runs unchanged via `handoff_dir`/manifest pointers (methods_log M2).
- v0.4 — Review round 1 (Claude verification 2026-09-02 + Ethan's rulings 2026-09-03) + parent re-pin v0.13 → **v0.14.1**. Flag fixes: (1) D-AB5 anchor CORRECTED — the parent's realized behavior is **17.6% of unlocked** (190,845/1,081,885), not 15% (that is the additions share of TOTAL extent; the two referents diverge as the locked share grows, i.e. precisely in AB) — anchor X = 17.6, freeze still at AB-0a; (2) input count corrected 12 → **8 continuous + EFG block** (parent card-miscount echo); (3) directory contract = `analyses/alberta_prioritization/`, and `spec/methods_log.md` + `spec/results_log.md` created as the binding living registers (parent convention); (4) code provenance = **working-tree import + git SHA recorded in the manifest + clean-tree assert at AB-3** (no package pin — repo has no package machinery; same-tree imports are the campaign-wide pattern). Verification erratum logged parent-side (results_log R9.8): discretionary = 1,081,885 not 1,082,069; the parent g=10% union re-measured = EXACTLY 100.000% of discretionary cells; the v0.2 changelog line here inherited the bad figure. Re-pin deltas absorbed: **guarded sweeps added to AB-2/AB-4** (aggregate-band MGA for the estimand + per-block-floor MGA via `mga_block_floors` for the applied product; guarded ensemble F = the Tim-facing surface, both semantics reported); **E14-analog zero-solve check** at AB-2; **meaningful-target-window check** added to the S4-analog derivation and AB-0a (first-order here — AB banked shares make the co-capture+lock-in floor high; a θ-target under the floor does not bind, parent E10's θ10 case); C1/C3 compare **matching semantics only**; §4 disclosure now cites M3.6 (adopted parent-side); E17 context registered for reporting (20/40 EFGs >90% south of 53°N — much of that mass is in/near the AB strip; stratified representation targets remain the fenced applied-paper alternative, §11); deliverable map grammar follows `director_package_spec.md` (tiers-not-fences guardrail, pre-stated clustering procedure, guarded frequent tier ≥0.70).
- v0.3 — Parent accounting CONFIRMED from code (Ethan read-back): characterization on full 1.27M extent; targets value-counting (locked amounts bank; Y2Y banked shares 12.3–19.2%); budget 30%-total with locked area consuming RHS (191,029 of 381,874). D-AB5 re-resolved: escalation now expected — primary AB-L budget pre-registered as locked + X% of unlocked (additions semantics; X set at AB-0a, anchor 15% of unlocked ≈ parent realized discretionary share); infeasibility case (locked > 0.30·N) named. AB characterization table gains supplementary zero-solve columns `banked_capture` / `residual_target` / `pre_satisfied` (disclosure of the audit-unconstrained vs lock-conditional gap, first-order at AB banked shares). H-AB5 registered (alpine-affine features pre-satisfied at lock; additions driven by residual-target foothills features). Parent methods disclosure line mirrored (§4).
- v0.2 — CORRECTION: the parent formulation already locks in existing PAs (evidence: 1.27M total vs 1,082,069 discretionary cells ≈ 15% locked; MGA Hamming space and verdict rule defined on discretionary cells; E17-T1 "locked-PA baseline separated" convention). D-AB2 rewritten — lock-in is inherited, not a deviation; AB-L is the exact mirror and primary config; AB-U demoted to optional S0-only diagnostic (C3). D-AB5 narrowed — budget-lock semantics inherited from the parent pipeline (read from code at AB-0a, recorded, not re-decided); only a degenerate-headroom contingency remains open. Invented lock rule (0.5 majority) removed — parent rule inherited. C1 re-pointed to AB-L vs Y2Y clip on discretionary cells.
- v0.1 — Initial draft. Deviation register D-AB1–D-AB4; open decisions D-AB5 (budget semantics under lock-in) and D-AB6 (adjacency buffer distance); gates AB-0a…AB-5; data acquisition table; pre-registered transfer hypotheses H-AB1–H-AB4.

---

## 1. Purpose

Two audiences, one build.

**Applied (primary deliverable).** Priority landscapes for protection within the Alberta portion of the Y2Y region, per Tim Burkhart's confirmed scope: full Alberta Y2Y extent; public and private tenure considered, private ranchlands retained for the OECM pathway; no a-priori exclusions; existing parks/protected areas not to be surfaced as recommendations (satisfied by the parent formulation's inherited PA lock-in with masked display — see D-AB2, corrected v0.2); areas of interest reported as audit overlays (§6). Deliverables: locked-config F surface and anchor maps with PAs masked from display; tenure-split reporting (crown → protection candidates, private ranchland → OECM candidates); AOI capture columns.

**Methods (secondary, feeds applied paper only).** Scale-transfer characterization of the frozen Y2Y-wide protocol: (a) does the feature-behavior regime change at subregional extent (§5, H-AB1–H-AB3); (b) do zoomed-in priorities spatially concur with the Alberta clip of zoomed-out priorities (§7, C1); (c) does plateau richness survive a smaller, more disturbed, more constrained extent (H-AB4).

## 2. Deviation register

Everything not listed here is identical to parent v0.13 by construction (library import + config).

| ID | Deviation | Detail |
|---|---|---|
| D-AB1 | Extent | Alberta portion of Y2Y region boundary. Planning-unit grid is the **exact subset of the Y2Y-wide grid** (same cell IDs, same alignment, ESRI:102008) — never re-snapped or re-gridded, so every cross-solution comparison is cell-exact. Cells intersecting the AB boundary follow the parent's boundary-cell rule unchanged. |
| D-AB2 | PA lock-in (CORRECTED v0.2: inherited, not added) | The parent formulation locks in existing PAs (discretionary-cell machinery; E17-T1 locked-PA baseline). **AB-L (locked) is therefore the exact procedural mirror and the primary config**, inheriting the parent's lock rule and PA layer (clipped to AB) unchanged. The only D-AB2 deviation is presentational: applied outputs mask locked cells so only additions display. Rationale for lock-in over Tim's literal "exclude" stands and is now also a mirror argument: hard exclusion would remove PA feature amounts from target accounting, so alpine EFGs / macrorefugia / high-intactness read as unmet and the additions map re-imports the rock-and-ice bias. **AB-U (unlocked)** is a genuine deviation and is demoted to an optional diagnostic — S0 cells only — feeding C3 (how much priority structure the existing PA network absorbs vs reorganizes). |
| D-AB3 | Tenure attribute join | Post-hoc reporting only; **never** enters the objective, constraints, or costs in v0.x (jurisdiction-blind principle; a tenure-weighted cost variant is future work, out of scope §11). Classes: `crown_green` (Green Area), `crown_disposition` (grazing/other disposition polygons), `private_presumed` (White Area minus crown dispositions minus PA), `private_ranchland` (private_presumed ∩ grassland/pasture cover), `pa_locked`. Reporting split: crown selections → protection candidates; private_ranchland selections → OECM candidates. **The private-lands ESTIMATE equation (v0.4.4, Ethan 2026-09-03; `03_ab0a_tenure_aoi`), per 1 km cell, centre-in-polygon rule:** `pa_locked = PA`; `crown_green = ¬PA ∧ Green`; `crown_white_ind = ¬PA ∧ White ∧ CROWN_IND`; `private_presumed = ¬PA ∧ White ∧ ¬CROWN_IND`; `private_ranchland = private_presumed ∧ ranch_frac ≥ 0.5`, where `CROWN_IND = active Crown Land Reservation ∨ PLUZ ∨ Disposition (when a licensed layer is present)` and `ranch_frac` = GVI ranchland fraction (native-upland site types + tame pasture/hay) where GVI exists, else AAFC Annual Crop Inventory 2024 fraction of classes {110, 122, 50}. Without dispositions (D-AB8 on hold), `crown_white_ind` stands in for `crown_disposition` and the private classes are **over-counts by the leased share — disclosed on every tenure table**; the ranch_frac threshold is reported at 0.3/0.5/0.7; the GVI→ACI seam (~52.5°N) is disclosed. At 1 km every class means "mostly", never a parcel. |
| D-AB7 | Presentation scale (added v0.4.1, Ethan 2026-09-03) | **The one place the applied layer diverges from the parent by design.** The analysis grid, estimand, bands, and thresholds are mirrored unchanged; but the AB extent is ~10× smaller, so the parent's presentation aggregation (`director_package_spec.md`: ~250 km² hexes, 100 km² minimum clusters, top-5–7 by area) would collapse Alberta into a handful of blobs. AB-5 sets its own aggregation appropriate to the AB scale — smaller hexes (or none: 1 km cells may be legible directly), a smaller minimum-cluster size, and an "opportunities" framing sized to what Tim's program can act on (parcels-to-landscapes, not sub-regions). Parameters are chosen at AB-5 with disclosure, never tuned to a cluster count (parent rule 9 carries); the clustering *procedure* (threshold at the guarded frequent band → closing → components → minimum size → naming) is inherited, only its constants change. Recorded here so it is a decided deviation, not drift. |
| D-AB9 | Biodiversity block kept whole under an R3 flip (Ethan ruling 2026-09-03) | AOH mammals is R3-inexpressible in Alberta (leverage 0.070 < λ). The mirror-literal consequence — mammals to the OUTSIDE set, the biodiversity block and S3 carried by birds alone — is REJECTED: a "biodiversity-forward" scenario levered on birds only misrepresents the elicited value. Ruling: **both AOH members stay in the block or neither does; chosen = both stay.** The block's share is split within-block as the parent (equal), weights derived by the same `scenario_weights` formula; mammals' derived weight is therefore large and provably inert (its captured fraction is confined to 0.33–0.49 whatever the weight — the R3 mechanism), which is DISCLOSED in T1 and in the methods text rather than "fixed". Rationale mirrors the parent's E17 ruling (no post-results value surgery; PROACT values are not deleted because the data cannot express them) and the parent's S5 precedent (an inexpressible push is still run and reported). Consequence: 14-formulation structure intact; S3's realized lever is birds + an inert mammals term. |
| D-AB10 | Applied band g = 2% (Ethan ruling 2026-09-03, Gate AB-2 report-back (a)+(b)+(c)) | At g = 5% the Alberta near-optimal band admits complete turnover of the additions (D ≈ 1, empty frequent tier — R5); the parent's v0.13 rulings name tighter g as the sanctioned cluster-drawing dial. **Applied layer: every frequency product (the ensemble F AND each formulation's own f) is computed at g = 2%**; the 5% band remains the estimand for the methods mirror (C1, E1/E3/E11-analogs) and is reported as measured. Both semantics (aggregate / guarded) are run at both bands; the guardrails' inertness here (R5.4) is disclosed, not hidden. The D-AB5 v2 nesting test is evaluated at the tightest band where both levels have non-empty cores (2%), threshold 0.80 unchanged; the primary budget level follows that result, defaulting to B if the 2% test is also vacuous. **Deliverable definition (Ethan): not only the full-ensemble F — the per-scenario frequency surfaces of the value-forward formulations (S1 core habitat, S2 connectivity, S3 biodiversity, S4 carbon; climate levels pooled unless they diverge) are first-class products**, the AB analogue of the director package's Act 2, each with its own frequent tier and clusters. |
| D-AB4 | AOI audit columns | Per-AOI capture % and mean F (locked config), `carroll2018_pctl`-style — never lock-ins. AOI-1: parks/wildlands adjacency buffer (distance = D-AB6). AOI-2: Upper Smoky SRP Nature-First Zone, computed **NFZ minus PA compilation** to isolate the novel area (NFZ includes Willmore/Kakwa); area from shapefile, not press figures (sources conflict: 3,200 vs 2,200 km²). AOI-3: "west of Grande Cache" — DEFERRED to reporting stage per Ethan 2026-09; candidate polygons identified from results geography, confirmed with Tim by map, then added as a column (zero solves). |

Explicitly NOT deviations: feature stack (original eight + 40 EFGs), budget percentage (30%, but see D-AB5), scenario structure S0–S5, climate axis (SSP245/SSP585, 2071–2100, constant-influence-per-scenario with per-cell weight re-derivation), estimator (`mga_maxham_v1`, k=50, g=5%), f(g) grid on reference cell, numerics standards (opt_gap 1e-4, NumericFocus 2, LP twin per certified MILP, dust rule), presentational bands, CRS.

## 3. Open decisions (resolve before AB-1)

**D-AB5 (re-resolved v0.3) — Budget semantics.** Parent semantics CONFIRMED from code: budget is 30%-total (RHS = 0.30 × 1,272,914 = 381,874 cells), locked cells sit in the A-matrix with lb=1 and consume RHS (191,029 locked; 190,845 discretionary selected). In the AB extent the mountain-park estate is concentrated, so the locked share will be a multiple of the parent's 15%; the inherited referent is expected to fail here — either infeasible (locked count > 0.30·N_ab: lb=1 exceeds RHS; check this FIRST at AB-0a, before any solve attempt) or near-trivial (sliver of additions). **Pre-registered resolution: primary AB-L budget = locked + X% of unlocked area (additions semantics). X is set at AB-0a with disclosure; anchor X = 17.6% of unlocked — the parent's realized fill rate of available land (190,845 discretionary selected ÷ 1,081,885 discretionary cells; corrected v0.4). The 15% figure is the parent's additions share of TOTAL extent — a different referent whose gap vs the fill rate scales with the locked share and is not negligible at AB lock levels.** Report alongside it, at zero solve cost, the factual current-protected share of the AB extent against the 30×30 lens — if that share is at or near 30%, that is itself a Tim-facing headline (the Alberta strip is near the areal target; the live questions are composition and additions, which the OECM track reinforces: OECMs are additions by definition). C1 caveats the changed budget referent wherever it applies.

**D-AB5 v2 (Ethan ruling 2026-09-03) — TWO-LEVEL BUDGET AXIS, pre-registered before any solve.** Ethan's applied constraint: ~10,000 km² of new protection in Alberta is not realistic now. Rather than choose between the derived mirror budget and a realistic one, both are run and the data decide which is primary:
- **Level A (mirror)** = locked + X·unlocked with X = 0.1764 → 38,055 cells (44.7%), additions 10,083 km² (`ab_extent_v1.json`).
- **Level B (realistic envelope)** = locked + (X/2)·unlocked → additions ≈ 5,041 km² (≈ 8.8% of unlocked, ≈ 38.8% of extent). Written to `spec/ab_budget_levels_v1.json` by `04_ab0_scenarios`.
- **Nesting test (AB-1/AB-2, S0 reference formulation at both levels; anchors + guarded MGA sweeps):** N = |core_B ∩ core_A| / |core_B|, the overlap coefficient of the guarded frequent tiers (F ≥ 0.70, discretionary cells). **Frozen rule: N ≥ 0.80 ⇒ NESTED — level A stays primary (the tiers of the wide envelope deliver the tight-envelope answer; the report draws the 5k envelope on them); N < 0.80 ⇒ NOT NESTED — level B becomes primary for the applied deliverable (full formulation set re-run at B; hours) and level A remains the methods mirror for C1.** Reported alongside, not ruled on: per-block captures at B vs the anchor at A; the same statistic on the unguarded tiers. Rationale for 0.80: four in five of the tight envelope's core cells already inside the wide envelope's core is the point at which the wide run's ranking is a usable proxy; a round number chosen before measurement. Min-shortfall is not nested in general (tight budgets buy dense claims only; loose ones reach diffuse values), so this is measured, not assumed.
- §11's "multi-budget beyond D-AB5" fence is amended to exactly these two levels.

**D-AB8 (opened v0.4.1) — `crown_disposition` without open disposition data.** Row 5's polygons are restricted (Altalis DIDs, for-fee); without them, `private_presumed` = White Area − PA is contaminated by crown grazing leases inside the White Area. Options, for chat: (a) a Y2Y/partner-licensed DIDs copy (likeliest — Tim's program works this tenure daily) → `data/tenure/dids_dispositions.gpkg`; (b) Altalis purchase; (c) a data request to `PGC.Data@gov.ab.ca`; (d) proceed with the contaminated class and disclose it (the OECM split then over-counts private ranchland by the leased share); (e) **academic route (checked 2026-09-03):** DIDs is in the AltaLIS Post-Secondary Education Data Partnership / GEODE consortium held by Alberta institutions (U of A `data@ualberta.ca`, U of C SANDS `maps@ucalgary.ca`, U of L, SAIT) — but the licence is "educational use only", signed per user, restricted to that institution's faculty/staff/students, and "cannot be shared with 3rd parties"; an out-of-province institution (e.g. UBC) is not a member. Usable only if a member-institution researcher is a bona-fide collaborator AND the library confirms an applied Y2Y deliverable fits "research"; the raw layer could never be handed to Y2Y — only class-level derived tenure shares. Tenure reporting (AB-5) waits on this; nothing upstream does.

**D-AB6 — Adjacency buffer distance (AOI-1). TABLED (Ethan 2026-09-03).** Not frozen before solving; instead, proximity of the result clusters to the existing estate is read post hoc from the AB-4 surfaces (distance-to-PA distribution of the guarded frequent tier and of each named cluster — zero solves), and a buffer distance is chosen, if one is still wanted, with that reading disclosed. The pre-registration protection this loses (a distance tuned to the result) is accepted because AOI-1 is a reporting overlay, not a formulation input.

## 4. Extent-relative targets (declaration)

All targets, leverage values, θ-tails, and a_min/t_min tests are computed on Alberta-extent feature totals. A target of t on a feature means t of *Alberta's* amount of that feature, not the Alberta share of the Y2Y-wide amount. This is intended: the Alberta solve is a different problem, not a crop of the Y2Y solution — the C1 comparison (§7) exists precisely because the two can disagree. Consequence for the dust rule: the <1e-9-of-feature-total threshold re-applies against Alberta totals; per-feature drop counts disclosed fresh (parent §2.6.2 discipline).

**Audit-space vs lock-conditional disclosure (mirrored from parent; adopted parent-side as M3.6, cited v0.4).** Characterization quantities (leverage, cap_min/cap_max, two-regime swing, θ-tails) are computed over the unconstrained selection space of the full planning-unit set, locked cells included; all solves are lock-in-conditional. Under lock-in the feasible capture interval per feature is [banked + poorest-discretionary-fill, banked + best-discretionary-fill] — floor above audit cap_min, ceiling generally below audit cap_max — so intended influence is a budget-capacity currency and realized influence operates on the lock-conditional feasible set. Estimand choice, not inconsistency; intended and realized reported side by side, no iteration against realized swing (parent v0.10 ruling). Because AB banked shares will far exceed the parent's 12.3–19.2%, this gap is **first-order here**, so the AB characterization table carries three supplementary zero-solve columns computed from the audit objects plus the PA mask: `banked_capture` (feature amount inside locked cells, as share of AB total), `residual_target` (max(0, t − banked_capture)), and `pre_satisfied` (banked_capture ≥ implied target). The primary R1–R4 characterization remains on the parent convention — required for C2 comparability; the supplementary columns are the disclosure made quantitative and the explanatory spine of the applied report (the additions map is driven by residual-target features).

## 5. Gate AB-0a — Feature characterization audit (full re-run)

R1–R4 verbatim on the clipped extent, frozen constants, all inputs (8 continuous + the EFG block; count corrected v0.4), feature cards regenerated, budget-independent audit objects rebuilt for the AB extent. Every derived target additionally passes the meaningful-target-window test (E10, mirrored v0.4) against the AB co-capture + banked floor. Class assignments follow the rules wherever they land — **class flips relative to the parent table are results, not problems**, and the AB-vs-Y2Y characterization table is the first headline comparison (C2). Pre-registered directional hypotheses (stated now so they cannot become post-hoc stories):

- **H-AB1 (contrast recovery).** The four Y2Y-wide low-contrast, weight-insensitive layers (intactness, macrorefugia, connectivity, AOH) regain spatial contrast in Alberta. Strongest expectation: gHM intactness — the foothills disturbance gradient is steep, so leverage should rise materially from 0.042; if it crosses out of R3-inexpressibility, S5 (intactness-forward) becomes expressible at this extent and D3(b)'s published-inexpressibility finding acquires a scale-dependence footnote. That flip alone would justify the exercise.
- **H-AB2 (EFG contraction).** The EFG set present in Alberta is much smaller than 40; several survivors sit near a_min = 0.5% of the (smaller) extent and R1/R2 dispositions will differ. Expect a shorter locked-adequacy foundation and possibly a larger unsaturated-disclosure set.
- **H-AB3 (carbon regime shift).** m_soc's concentrated-satiating classification is driven by boreal/foothills peat-adjacent tails that are partly inside Alberta; whether the θ-tail crossing and implied target survive the clip is genuinely uncertain — no directional prediction registered, which is itself the registered statement.
- **H-AB4 (plateau richness, tested at AB-2).** Smaller extent + steeper disturbance gradients + (in AB-L) locked structure should *narrow* near-optimal freedom: expect D below the parent's 0.953 and C above 0.020, direction only. If AB-L turns plateau-poor (D < 0.10 or C > 0.90 under the parent's frozen verdict rule v2), the F surface for Alberta is closer to a single map than an ensemble — which is itself the decision-relevant answer for Tim (little implementation flexibility) and must be reported as such, not treated as a failure.

- **H-AB5 (banked pre-satisfaction, v0.3).** The AB locked estate is refugia-rich and alpine-EFG-rich (parent banked shares already peak on refugia at 19.2% with only 15% of cells locked); expect several alpine-affine features (macrorefugia, high-elevation EFGs, intactness) to arrive `pre_satisfied` — zero residual target pull, constraint slack at the root — while foothills-concentrated features carry near-full residual targets into a thin discretionary pool. Prediction: the additions map is driven by residual-target features on the disturbance gradient, and this, not weight choice, is the first-order determinant of AB-L selection. Verdict from the supplementary columns at AB-0a, before any solve.

Gate AB-0a also measures: locked-cell count and share (D-AB5 infeasibility check first, then X), per-feature `banked_capture`/`residual_target`/`pre_satisfied`, tenure class shares, AOI overlay areas.

## 6. Scenario set, ensemble, and outputs

- `scenarios_ab_v1.json` derived by parent §3.1 rules from the AB-0a characterization: S0 equal-discretionary-influence-per-block (joint block accounting, two-regime swing on AB-extent layers); S1–S3 block-share doubling; S4 carbon-forward by the parent's θ-relaxation + block-doubling recipe *re-derived on AB tails* (parent's t=0.552 is a Y2Y number; the AB analog comes from the AB audit) **with the meaningful-target-window check (v0.4): the AB θ-target must clear the AB co-capture + banked floor, or it is a non-binding decoration (parent E10's θ10 case) — first-order here because banked shares are large (H-AB5)**; S5 status contingent on H-AB1. Climate axis crossed as parent. Formulation count 14 if S5 remains inexpressible-disclosed; 16 if S5 activates (then reconcile against parent structure in the report).
- AB-L runs the full formulation set; AB-U runs S0 formulations only (C3 diagnostic, optional). Compute is not a constraint: the AB extent is roughly an order of magnitude fewer planning units than the parent's 1.27M, so anchors are expected in seconds and full MGA ensembles in minutes per formulation; budget the wall generously at parent-per-formulation rates anyway and report measured.
- **Dual-semantics ensemble (v0.4, mirroring parent E14/E15 rulings):** per formulation, the aggregate-band MGA sweep (the Claim-A estimand and the C1/C2/H-AB4 currency) AND the per-block-floor guarded sweep (capture_b ≥ 0.95·anchor_b, `mga_block_floors`; anchors/twins stand — floors are anchor-relative, no anchor re-solve). Guarded ensemble F = the applied deliverable ("no value theme left >5% behind"); both semantics reported side by side (the E15 doubling table is a Tim-facing slide in its own right).
- Estimand: F_i per config, parent §4 unchanged; D quoted from MGA only (E12 ruling). E4's f(g) grid on one reference formulation per config.
- Applied outputs (AB-L): guarded F surface with `pa_locked` masked (aggregate-band F in the appendix); anchor maps masked; T1-analog scenario table; tenure-split selection summaries; AOI table (capture %, mean F, sensitivity rows per D-AB6); one-page map set for Tim following the `director_package_spec.md` grammar — guarded frequent tier ≥0.70, pre-stated clustering procedure, tiers-not-fences guardrail sentence, plain-language captions (no estimand jargon in the deliverable layer).
- **Precedent conventions mirrored from the parent's v1.3 section (v0.4.2; Jung 2021 / Buenafe 2023 / Currie, Liang & Snider 2025), scale-independent so adopted verbatim:** (i) **tier-achievement figure** (zero-solve): capture per PROACT block by cumulative tier — core / core+scenario / opportunity — with anchor captures as reference lines, paired with the T-D2-analog tier accounting under both semantics; (ii) **% area annotated on every scenario map** and an **inset histogram of guarded F** on the core map; (iii) **the Act-1 map as a Currie-style (a)/(b) pair — but with the AB twist D-AB2 requires: (a) = additions-only F with `pa_locked` MASKED (the recommendation view), (b) = the same F with the locked estate drawn as a distinct context layer (never as a recommendation)** — the pair discharges Tim's "don't surface existing parks" constraint while keeping the genre directors recognise; (iv) **agreement matrix** (pairwise Jaccard between formulation anchors) in the appendix — the E11-analog already computed at AB-4; (v) **T-D4-analog: tier area by Alberta Natural Region / Subregion** (the ecozone analogue directors expect; `input_data/ab_foothills/foothills.gpkg` is the Foothills region already on disk — acquire the full Natural Regions layer, open GoA) **and by tenure class** — the AB-specific stratification; (vi) the E17 precedent line (the national analysis found the same species-pull-south tension) carried into the AB report's representativeness context. **Scale sentence, AB version (D-AB7):** the parent displays at ~250 km² hexes ≈ the national analysis's 100 km² planning unit; the AB extent is only ~1,300 such hexes, so the display unit here is at or near the 1 km analysis resolution (or hexes an order of magnitude smaller than the parent's) — Currie's 100 km² hexbins are the precedent for coarsening, not a target; constants set at AB-5 with disclosure.

## 7. Comparisons (the "zoom-in" question, made concrete)

| ID | Comparison | Instrument | Reading |
|---|---|---|---|
| C1 | AB-L anchors & F vs Y2Y-wide anchors & F clipped to AB — both locked; computed on discretionary cells with the locked-PA baseline separated (parent E17 convention; the two configs share the same locked set by construction, so discretionary cells align); **matching semantics only (v0.4): aggregate-band F vs aggregate-band F, guarded vs guarded — never crossed** | **Overlap coefficient** per scenario formulation (Jaccard only as supplementary — set-size artifact when the clip's selected share ≠ 30% of AB; parent Morris lesson) + F–F rank correlation on shared cells | Do subregional priorities concur with the AB portion of whole-region priorities? Divergence localizes where extent-relative targets bite. |
| C2 | AB-0a characterization table vs parent §2.5 table | Side-by-side: leverage, θ-tail, class, lever per feature; flip list | Does the feature-behavior regime change with scale? (H-AB1–H-AB3 verdicts.) |
| C3 | AB-L vs AB-U (S0 cells only; optional) | Selection delta maps; per-feature capture deltas; F delta | How much priority structure does the existing PA network absorb vs reorganize? The additions logic, isolated. |
| C4 | AOI columns (AB-L) | Capture % and mean F per AOI vs extent-share null | Does the tool independently find what Y2Y already believes matters? Low NFZ capture is informative, not embarrassing (zone drawn for caribou + existing parks, not complementarity) — pre-stated so it can be reported straight. **Language rule mirrored from the parent's IPCA overlay (v0.4.2, binding): ALIGNMENT, not assignment — AOIs are not locked in, so overlap is independent convergence; the analysis does not set priorities inside an AOI, it shows that where the AOI advances, named value clusters would be protected. Same call-out styling on the Act-2 maps.** |

## 8. Data acquisition

| # | Layer | Role | Source | Status |
|---|---|---|---|---|
| 1 | Y2Y boundary, AB clip | Extent | In hand | Clip + grid-subset only |
| 2 | Feature inputs (8 continuous + 40 EFGs) | Features | In hand (Y2Y-wide stack) | Clip to grid subset; re-run dust rule |
| 3 | PA compilation (CPCAD / Y2Y PA classification incl. AB parks+PLUZ from GeoDiscover) | AB-L lock-in; NFZ subtraction; tenure class `pa_locked` | In hand | Verify AB completeness (national + provincial + wildland + PLUZ conservation designations) against CPCAD before lock rule applies |
| 4 | Green Area / White Area boundary | Tenure: crown vs settled split | Alberta EPA, OGL-A, ArcGIS REST (no token), `GWA_NAME` (verified 2026-09-03) | `data/acquire.py` |
| 5 | Public land dispositions (grazing et al.) | Tenure: `crown_disposition` | **NOT OPEN (scouted 2026-09-03): GeoDiscover "Agriculture/Application Disposition" are Protected A, REST token-gated; distributed for-fee by Altalis (DIDs, `DISP_TYPE` GRL/PGL/GRP/PGP/FGL/FGA/GRR/FDL/CUP/PHP)**. Open partials: Crown Land Reservations (not leases), Grazing Rental Zone (rate zones) | **BLOCKED — decision (D-AB8, §3)** |
| 6 | Grassland/pasture cover (GVI in White Area; AAFC fallback) | Tenure: `private_ranchland` classifier | GVI (Alberta EPA, OGL-A, FGDB; covers 48.9–52.5°N only — Sites `SITE_TYPE` native upland 11–24 + PN/PI) + AAFC Annual Crop Inventory 2024 AB (classes 110/122/50) north of GVI coverage (verified) | `data/acquire.py` |
| 7 | Upper Smoky SRP spatial data (`US_SRP_Zones` package) | AOI-2 (NFZ) | Alberta EPA extranet zips (OGL-A, SHP, EPSG:3400; revised 2026-07-17; zone field name unverified until opened) | `data/acquire.py` |
| 8 | AltaLIS cadastral (ETM) | Parcel-level ranchland (optional refinement) | AltaLIS (paywalled; possible UBC access) | Deferred — class-level tenure (rows 4–6) suffices for the OECM reporting split |

Naming note for all documents and the Tim-facing report: the plan is the **Upper Smoky Sub-Regional Plan** (Tim's "Upper Little Smoky" corrected; likely conflation with the Little Smoky caribou range).

## 9. Gates

| Gate | Content | Exit criterion |
|---|---|---|
| AB-0a | Grid subset; dust re-run; full R1–R4 audit (parent convention) + supplementary banked/residual/pre_satisfied columns; cards; locked count & share (infeasibility check first), tenure shares, AOI areas | Audit frozen; H-AB1–H-AB3 and H-AB5 verdicts recorded; D-AB5 X set with disclosure; D-AB6 frozen |
| AB-0 | `scenarios_ab_v1.json` frozen; comparison solves (a-series analogs) on AB extent, both configs | Targets bind at kink; lever-principle reproduction holds at AB extent |
| AB-1 | S0 anchors ×2 configs ×2 climate; T1-analog; LP twins | Certified; captures in pre-stated bands or reported-as-failed (parent honesty convention) |
| AB-2 | MGA pilot, reference formulation (AB-L; AB-U only if C3 activated); verdict rule v2 (parent hash, unmodified) on the aggregate band; E14-analog zero-solve check on the members (do AB members sacrifice whole blocks inside the band?) + guarded pilot at the same formulation (v0.4) | H-AB4 verdict; plateau-poor outcome is a finding, not a fail; guarded-vs-aggregate frequent-tier delta recorded |
| AB-3 | Manifest freeze (schema = parent §9 + `config` ∈ {ab_u, ab_l}, `extent_id=ab_y2y_v1`, `mirror_spec_version=v0.14.1`, `lock_rule`, `budget_semantics`, `guard_semantics` per artifact, `pipeline_git_sha` with a clean-working-tree assert at freeze time — v0.4) | `manifest_freeze.sha256` |
| AB-4 | Full run AB-L, both semantics per formulation (aggregate-band + guarded sweeps; + AB-U S0 subset if activated); C1–C4 measured under matching semantics | Guarded and aggregate F surfaces delivered; comparisons in report |
| AB-5 | Applied reporting: masked maps, tenure split, AOI table incl. AOI-3 candidates → Tim map-confirm | Tim-facing packet out; AOI-3 column added post-confirmation |

## 10. Directory / file contract

```
analyses/alberta_prioritization/               # sibling of analyses/y2y/ (path fixed v0.4)
  spec/alberta_prioritization_spec.md          # this file (changelog lives in its header)
  spec/methods_log.md  spec/results_log.md     # BINDING living registers, parent convention (v0.4)
  spec/manifest.csv + manifest_freeze.sha256
  spec/scenarios_ab_v1.json
  audit/feature_cards_ab/
  audit/audit_objects_ab/
  data/tenure/  data/aoi/  data/pa/            # acquired layers, provenance sha256s
  runs/<config>/<formulation_id>/anchor/ mga/ mga_guard/ twin/
  analysis/c1_concordance/ c2_audit_compare/ c3_lock_delta/ c4_aoi/
  figures/  report_tim/
```
**Code provenance (v0.4 ruling):** notebooks bootstrap to the repo root and import the live pipeline modules from the same working tree (the campaign-wide pattern — `config`, `leverage_core`, `ensemble_core`, `prioritizr_core.R`, `mga_core.R`); no package pin and no code copied into this folder. The manifest records `pipeline_git_sha`, and the AB-3 freeze asserts a clean working tree so the SHA is a faithful pin; any later pipeline edit that touches an AB run is a manifest re-freeze (parent §9 discipline). Upstream spec amendments propagate by re-pin (mirror declaration).

## 12. Build record (notebooks, run by Ethan in order; kernels as the parent campaign)

| nb | gate | kernel | content |
|---|---|---|---|
| `01_ab_extent_stack` | AB-0a (i) | y2y-geo | AB extent = parent PU ∩ Alberta polygon (centre-in-polygon rule, same as the parent's rasterization; the Y2Y-side rule incl. the 20 km buffer is inherited through the parent PU); builds the AB hand-off stack `input_data/aligned_stack_ab/` = parent stack masked on the SAME grid (no crop, no re-snap ⇒ cell-exact), dust re-run vs AB totals, EFG drop rule re-applied, PA mask + climate realizations mirrored; lock accounting, D-AB5 infeasibility check FIRST, X derived from parent artifacts; writes `spec/ab_extent_v1.json` + AB manifest |
| `02_ab0a_audit` | AB-0a (ii) | y2y-geo | R1–R4 on the AB stack at the parent's 30% convention (C2 comparability) + leverage at the effective AB budget as a supplementary column; F8; feature cards; archive; `banked_capture`/`residual_target`/`pre_satisfied`; meaningful-target-window proxy; C2 flip list; H-AB1/2/3/5 verdict block |
| `03_ab0a_tenure_aoi` | AB-0a (iii) | y2y-geo | tenure classes (D-AB3) + AOI overlays (D-AB4; D-AB6 tabled) once §8 rows 4–7 are acquired — does not gate AB-0 |
| `04_ab0_arms` | AB-0 (i) | y2y-r | a-series analogs at budget level A, w = t, Gurobi binary: a0 control (measures every feature's co-capture + lock-in floor), a1 protocol (m_soc 0.322 — expected non-binding, pre-satisfied), a4 pull-check (connectivity 0.80, unreachable), a5 floor probe (m_soc 0.772 = θ 2×) → `runs/ab_l/gate0/<arm>/` |
| `05_ab0_scenarios` | AB-0 (ii) | y2y-geo | G0 checks re-read for a pre-satisfied target; **the S4 ladder** (below); a4 objective-equivalence; carbon split diagnostic (AB caveat); S0–S5 derivation with D-AB9; 245-level leverage + Jaccard; writes `scenarios_ab_v1.json` + `ab_budget_levels_v1.json` |
| `06_ab1_anchors` | AB-1 | y2y-r | engine anchors (binary, certified) + Gurobi-proportion twins for S0 × {A, B} × {585, 245} and the S4 pilot at A → `runs/ab_l/<level>/<formulation_id>/{anchor,twin}/` |
| `07_ab2_mga` | AB-2 (i) | y2y-r | reference formulation at A and B: MGA anchor (drift-checked vs 06), aggregate sweep g=5% k=50, guarded sweep (per-block floors 0.95), f(g) grid {2, 10%} at A |
| `08_ab2_analysis` | AB-2 (ii) | y2y-geo | verdict rule v2 (parent hash asserted) → H-AB4; E14-analog; **D-AB5 v2 nesting test** → primary level; f(g) erosion; S4 pilot binding + θ-tail band; T1-analog; `spec/gate_ab2_verdicts.json` |
| `09_ab3_freeze` | AB-3 | y2y-geo | manifest freeze (14 formulations at the primary level; A/B per the nesting verdict) |
| `10_ab4_ensemble` | AB-4 | y2y-r | full run, both semantics |
| `11_ab4_analysis` | AB-4 | y2y-geo | F surfaces, C1–C4 |
| `12_ab5_report` | AB-5 | y2y-geo | D-AB7 aggregation, masked maps, tenure split, AOI table, Tim packet |

**S4 target ladder (pre-registered v0.4.3, operationalising D3 + parent E10; reference arm CORRECTED v0.4.5 after the first 05 run — M5.7):** S4's m_soc target = the first rung of θ ∈ {2, 1.5, 1.2} (AB archive: 0.772 / 0.848 / 0.877) whose target exceeds the **measured** level-A co-capture floor by ≥ 0.01. The floor is a1_protocol's realized m_soc capture (its 0.322 target is pre-satisfied, so above it the carbon term exerts no pull — the E10 definition of co-capture + lock-in), NOT a0's equal-weight capture, which includes carbon's own pull (v0.4.3 named a0 in error; the pre-registered stop fired on the first run and was taken to chat as specified). Measured: a1 0.744 (proxy 0.765; a0 0.867) → θ 2× (0.772) clears by 0.028, and a5_floor certified it binds at the kink (0.7720). The zero-solve proxy (0.765) is a lower bound on that floor; the solve replaces it. The AB-1 S4 pilot must certify binding (capture at the kink); if it does not bind under S4's doubled-carbon weights, step up one rung — pre-registered fallback, not a new decision. Level B's floor is measured at AB-1 and S4's target at B, if B becomes primary, follows the same ladder on B's floor.

Engine reuse: `config.ANALYSES["ab_y2y"]` + `config.AB_HANDOFF_DIR`; the R notebooks call `config.write_manifest(analysis="ab_y2y", handoff_dir=AB_HANDOFF_DIR, manifest_path=…)` then `pr_setup(<ab manifest>, PROJ)`; no engine code changes.

## 11. Out of scope (fenced)

OECM policy analysis and parcel-level ranchland identification (class-level tenure only). Tenure-weighted costs (future variant; would break jurisdiction-blindness of v0.x). Corridors v2 / least-cost machinery (separate track). "West of Grande Cache" boundary definition before results exist (D-AB4/AOI-3 sequencing). Paper-1 claims (parent owns the estimand novelty; anything AB-derived is applied-paper material). Multi-budget beyond D-AB5's resolution. Ranchland OECM feasibility scoring. **Layer/target surgery flagged parent-side as applied-paper material (v0.4): AOH replacement (rarity-weighted richness or species-level AOH; parent v0.13.1) and regionally stratified representation targets (parent E17 remedy alternative) — both would break the mirror and are future D-AB deviations requiring a new decision, not v0.x scope; the E17 context (20/40 EFGs >90% south of 53°N, much of it in/near the AB strip) is reported, not acted on.**
