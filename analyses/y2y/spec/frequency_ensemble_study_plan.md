# Hierarchical Selection-Frequency Ensemble — Study Plan (Paper 1)

**Status:** v0.14.1 — E-round (E8–E17) COMPLETE and ratified; E15 guarded-sweep completion authorized (all 14 formulations; changelog + `director_package_spec.md` step 0). Rulings: E12 bracket accepted (no full-14 MAA; D quoted from MGA); E15 guardrailed semantics PROMOTED to applied headline (aggregate band remains the Claim-A estimand); E17 remedy = quantitative main-text disclosure + scenario reading + explicit Y2Y endorsement decision on the representativeness anchor (applied-paper stratified-representation alternative noted). Empirical phase CLOSED except the guarded completion run. Remaining: Gate 5 — director package (subordinate spec: `director_package_spec.md` v1.1), draft architecture, García-Quintas full text, Lehtomäki skim, fresh sweep at submission.
**Scope:** Single methods-forward paper. Value scenarios over PROACT-selected objectives aligned to Y2Y core mission (PROACT selected objectives; no weights/ranges were elicited). Robustness of prioritization claims + named forward scenarios (core-habitat-, connectivity-, biodiversity-, carbon-forward).
**Companion documents:** leverage/carbon-dominance analysis; novelty verification report (§12); gate0_reportback.md; gate2_reportback.md; gate4_reportback.md; e_round_reportback.md; **subordinate build spec: `director_package_spec.md` (v1.1, Gate-5 deliverable).**

## Changelog
- v0.14.1 — Cross-reference: the Y2Y director communication package is specified in the subordinate build spec `director_package_spec.md` (v1.1) — a Gate-5 deliverable consuming this study's artifacts; presentation decisions live there, methods decisions live here, ambiguous items logged in both. One such item logged now: **E15 guarded-sweep COMPLETION** (package-spec step 0) — per-block-floor MGA sweeps extended from {S0, S4} to all 14 formulations (~8–10 h serial; anchors, k-best pools, and twins stand; floors are anchor-relative), plus the queued guarded MAA spot-check on S0. This completes E15 from a 2-formulation demonstration to full-ensemble coverage; guarded ensemble F becomes a computed product.
- v0.14 — E-round ratified (R9.1–R9.7, M4.18, M3.6). (1) E12: f is instrument-robust (corr 0.851–0.964; frequent bands within ~5%; cores identical); D estimator-sensitive by definition (maximizing probe) — quoted from MGA only; 3-formulation bracket accepted, full-14 MAA declined; E3's conditionality caveat softened to cite the bracket; S2's weakest corr (0.851) noted as the diffuse-formulation self-consistency check. (2) E13: mechanism CONFIRMED — S4's f≥0.70 set 80.8% inside the m_soc θ-tail; S0/S2 high-f sets 4–5% tail / 1–4% spike; high frequency follows binding claims, not valued layers; Q2 explanation cleared for Discussion. (3) E14 trigger fired (~100% of members drop ≥1 block below 0.95× anchor — the aggregate tolerance routinely financed by sacrificing whole blocks) → E15: per-block floors DOUBLE the frequent tier (S0 11,247→23,108 km²; S4 28,748→34,787) at ~4% diameter cost; E15b (per-value floors) adds ≈nothing — COMMITMENT SATURATES AT THE BLOCK LEVEL: the elicited granularity is the dial's natural resting point (retroactive validation of block accounting). RULING: guardrailed semantics PROMOTED to applied headline — F reported under both semantics, guarded frequent tier = the Y2Y deliverable, "no value theme left >5% behind"; aggregate band remains the Claim-A estimand (promotion is presentational; E14's pre-registered trigger legitimizes it); one guarded MAA spot-check on S0 queued. (4) E17: T2 — 20/40 EFGs >90% south of 53°N (median mean-latitude 48.6°N); T3 all pre-stated directions confirmed — EFG-out +2.11°N (Jaccard 0.695), the LARGEST single force; biodiversity-out +1.18°; carbon-out −1.18°; connectivity-out −0.96°. RULING: main-text quantitative disclosure + scenario reading + EXPLICIT Y2Y ENDORSEMENT DECISION — framing carries both truths: the 2° southern anchor was never explicitly endorsed AND it encodes real conservation logic (rare EFGs are southern because conversion pressure squeezed them there); remedy is handing the measured choice back, not deleting the doctrine; applied-paper alternative = regionally stratified representation targets. (5) E8 (satiation inertness at scale: Jaccard 0.996/0.994, capture exactly 0.3320), E9 (lever trilogy: weights-only drifts 0.462/dd 0.632; target precise 0.332/0.392; log-carbon destroys per-hectare value 0.266/dd 0.236 — screening prediction confirmed), E10 (θ3 parks exactly; θ10 does NOT bind — capture 0.229 vs target 0.121: MEANINGFUL-TARGET-WINDOW concept adopted into Methods — targets below the co-capture+lock-in floor are decorations; window priced by the Lorenz curve). (6) M3.6 lock-in disclosure adopted: PAs pre-bank 12–19% of each feature (m_soc 17.6 of its 33.2-pt target) and consume 50.0% of the 30% budget; audit capacities are lock-in-unconstrained while solves are lock-conditional — one mechanism behind R8.5's structural misses. (7) E16 resolved to the sanctioned F-guided clustering fallback (min-boundary MILP fails the compute gate at ~2.5M edge binaries).
- v0.13.1 — E17 added; EFG/AOH drop ruling (counterfactual, not deletion).
- v0.13 — Gates 3+4 ratified into the record (manifest sha d45668bb…; 13 open formulations, wall ≈22 h; s3_ssp585 pool 38/50 at 12 h limit disclosed as certified in-gap incumbents; pool difficulty formulation-specific, spread 79×). R8 headline results entered: ensemble always-band EMPTY (strongest tier frequent ≥0.70 = 6,816 km²); E1 mean |F−F_naive| = 0.169, 67% of ever-selected land shifts >0.1 (the correction k-best would have scored ≈0); E3 within/values/climate = 95.2/4.4/0.2% (ESTIMATOR-CONDITIONAL — within = MGA-extremes breadth; state every time quoted); every formulation plateau-rich (D 0.809–1.000; S5 hits D=1.000 — inexpressible push admits complete discretionary turnover); E7 outcomes-vs-places (captures in narrow ranges, maps at Jaccard to 0.373; S4@245 biomass tail 0.716 vs the 585-registered 0.75 band — reported as layer-sensitivity finding; crossed cells 0.584–0.697 complete the M6.7 dose-response: places needs target AND weights); E11 within-diameters EXCEED between-anchor distances — value disagreement fits inside any single position's near-optimal freedom; Δ: 156/182 ordered pairs mutually in-band (certified no-regrets pluralism), all 26 exceptions = anchors under carbon-forward objectives (Δ 0.085–0.093); Δ-diagonal self-check caught and fixed a layer-consistency bug (M5.11). NEW ANALYSES: **E12** bracketing estimator — random-objective-direction band sampling (MAA/SPORES lineage: Pedersen et al. 2021; Lombardi et al. 2020; Grochowicz et al. 2023 — verify cites; Brill/DeCarolis/Brunel remain the MGA parents), k=50, same band machinery, 3 formulations first (S0-ref, S2, S4) then decide full 14; report f and E3 under both estimators as the bracket. **E13** frequency-attribution diagnostics (zero-solve): f>0.7 overlays vs m_soc θ-tail / rare-EFG footprints / connectivity spike; f-by-value-decile curves (spread-vs-spike fingerprint); S2 manifest+realized-influence sanity check. Q2's counterintuitive cluster provisionally explained as binding-scarcity-vs-substitutable-emphasis (high f follows binding claims, not valued layers; connectivity's diffuseness spreads f thin, carbon's tail pins f≈1 on cells that co-carry connectivity value) — E13 must confirm before it becomes Discussion text. **E14** per-feature capture spread across existing 51-member sets (zero solves) → decides **E15** guardrailed-band sweep (per-BLOCK floors capture_b ≥ (1−g)·anchor_b as added rows; pre-registered secondary semantics; 2–3 formulations) — aggregate band REMAINS the primary estimand (faithful to elicited trade-offs). **E16** band-constrained delineation demo: min boundary length s.t. objective ≤ (1+g)·optimum (+E15 floors if adopted) — contiguity bought with proven plateau slack, NOT an objective penalty; 1–2 formulations, compute-gated (edge variables at 1.27M cells; fall back to F-guided post-hoc clustering). RULINGS: boundary penalty STAYS OUT (unelicited value with uncalibrated influence; 400× cost; post-results re-freeze); tilt magnitudes UNCHANGED (E3: dispersion is band width, not anchor position; frequency pins via binding scarcity, not weight; sanctioned dials for cluster-drawing = tighter g (per-formulation surfaces at g=2%) + E12 bracket reading). E8–E10 supplementary solves scheduled PRE-DRAFT. Terminology: "formulation" for design points, repo-aligned. Gate 5 parallel entry: write-up spine = two-instrument geometry + empty always-band + E1 (Claim A), E7/E13 (Claim B), E11 (Claim C/no-regrets); García-Quintas email out THIS WEEK; Lehtomäki skim scheduled.
- v0.12.1 — E11 added (two-level spread + Δ matrix).
- v0.12 — Gate 2a/2b ratified; GATE 3 AUTHORIZED. (1) S4 pilot PASS: m_soc tail 0.960, biomass tail 0.772 (band ≥0.75 both); biomass total moved only +1.9 pts while tail nearly doubled — carbon-forward pressure REDIRECTS capture dense-first (mechanism validated; F9 panel ready-made: 0.435/0.425 → 0.960/0.772). (2) Tail-mask contingency RESCINDED entirely (M4.14, Ethan's directive): no standing escalation authorization; a hypothetical future failure returns to design discussion; machinery archived (`archive/08b_contingency_tails_RESCINDED.ipynb`), quarantined layers retained as verified backup knowledge (both rare-attainable, exact frozen-T2 matches). v0.11 contingency language superseded. (3) MGA reference run: 150/150 members across g∈{2,5,10%}, zero duplicates, every band certificate binding exactly; iterations 10–16 s (faster than predicted); ensemble re-projection ~45–60 min/cell ⇒ ~10–14 h serial. Verdict rule v2 (frozen pre-run, hash v2_8db80fed1c702638): PLATEAU-RICH — D=0.953, C=0.020 at g=5%, passing thresholds (D≥0.10, C≤0.90) by an order of magnitude. Claim A carries; E1's expected effect restored to real. (4) f(g) headline: always-core 12.0% of selection at g=2% → 2.0% at 5% → ZERO at 10%, where the union covers essentially every discretionary cell (1,081,885/1,082,069). Logical status pinned: zero f=1 among 50 certified band members PROVES no universally-required cell (airtight direction); f=1 cores at tighter g are UPPER BOUNDS on the truly-required set — captions say "in every generated plan," not "every possible plan." Paper sentence ratified: "almost any cell CAN be part of a near-optimal plan; almost no cell is REQUIRED by one." (5) Two-instrument geometry promoted to thesis figure: needle-sharp unique optimum (k-best: 50 near-clones within 3.2e-6) on a wide shallow plateau (MGA: near-disjoint plans within 5%) — the certified optimal map is real and almost meaningless as a summary; E5 discharged by the contrast. Y2Y Discussion note: D=0.953 ⇒ two nearly non-overlapping 30% plans within 5% of optimal — implementation flexibility, measured. (6) Freeze confirmations: climate-cell mechanics as specified (constant intended influence across climate levels; per-cell weight re-derivation from layer-specific swing); E4 extra g-levels reference-cell-only; manifest parameters per report §4 (k=50, g=5%, MIPGap_dist 0.01, TimeLimit_iter 900, opt_gap 1e-4, NumericFocus 2, verdict-rule hash, per-cell artifact set, layer sha256s).
- v0.11 — Carbon-forward places-mechanism simplified per review: S4 = pure (w, t) on the EXISTING feature stack (m_soc t=0.552 + block-doubled carbon weights); masked tail features demoted from design element to pre-authorized escalation contingency. Rationale: (i) evidence that sufficient pull captures tails (biomass tail 1.000 at w=1 in a1; connectivity 0.980 at moderate w) — S0's tail skip was a low-pull condition, which carbon-forward reverses by construction; (ii) at t=0.552, off-tail co-capture becomes area-expensive, making dense-first the cheap shortfall path; (iii) surgical-vs-blunt: raised weights buy the tail by also buying diffuse biomass broadly — a cost in most scenarios but ON-MESSAGE in carbon-forward specifically; (iv) min-shortfall targets are pressure, not locks — v0.10's t=1.0 "locks" over-specified the places reading; the value statement is "a lot of the tail," not completeness. Procedure: one certified S4 pilot solve (≈55 s); pre-stated acceptance band tail capture ≥0.75 BOTH pools; pass → no formulation change, full stack symmetry restored, masked-layer re-audit off the critical path; fail → escalation fires (masked features at t≈0.8, re-audited under frozen rules, disclosed) — including the case where the required w_bio exceeds the doubled block allocation (blunt instrument can't reach the places value at acceptable influence cost). E7 S4 expectation updated from ≈1.0-by-construction to the acceptance band.
- v0.10 — Gate 2 ratified and pivoted. (1) D-A(a): within-cell estimator = Brunel-style diversity-controlled generation (max Hamming distance from incumbents s.t. objective ≤ (1+g)·certified optimum, direct Gurobi calls via the compiled-model machinery). Estimand RE-REGISTERED: f_{i,s} = membership fraction across k maximally-diverse g-optimal solutions — interval logic (which cells survive every corner of the band), deliberately not volume-weighted sampling (uniform vertex sampling intractable; volume weighting is plateau geometry, not decision relevance). g retained as a certified hard wall and lens parameter; E4's g-grid {2,5,10%} becomes the central f(g) robustness curve (core erosion with tolerance). New degeneracy-verdict rule to be pre-registered BEFORE Gate 2b on MGA-appropriate statistics (achieved Hamming diameter vs discretionary size; f=1 core share — NOT conditional-cell %, which MGA inflates by construction); Claude Code proposes thresholds. k-best pool retained one-per-cell in the ensemble: per-formulation optimum-uniqueness statement + the k-best-vs-MGA contrast discharges E5 as a by-product. M6.6 promoted to results inventory (PoolGap acts as bound never sampling target; the estimator demonstrated E5's enumeration-order bias on itself). (2) D-B: amount-vs-places became a SCENARIO AXIS. S0 = amount (unchanged; Gate-2 artifacts stand as the measured record). Carbon-forward = amount dial (θ 5×→3×, m_soc t=0.552) PLUS places locks [REVISED v0.11 → pilot + contingency]. M6.7 promoted to results inventory and reframed: "a total-capture target secures an amount, not places" is the empirical motivation for the places scenario; S0 tail expectation stays honestly-failed (~0.43). (3) CORRECTION: v0.9.1's "weight sets depth, not order" rationale REFUTED by M6.7 (joint scoring lets competition invert within-feature hotspot preference; biomass tail 0.425 at w=0.199, m_soc tail 0.435 with target binding) — removed; within-block mass-proportional split stands on the S0 amount decision, not the refuted argument. (4) Rulings: m_soc +0.089 realized-influence miss ACCEPTED as structural (satiating member realizes 100% of claim; diffuse members ~40–50% of capacity under competition — Claim C's caveat, now measured; iterate-once NOT invoked). Ensemble LP twins on Gurobi's LP path; HiGHS re-verification as 2-cell spot-check. (5) Costing: bundled rebuild = estimator swap + S4-only re-freeze (S0 untouched).
- v0.9.1 — Carbon within-block split made diagnostic-driven; θ-tail capture rate adopted as standing T1/E7 diagnostic. [Rationale text corrected in v0.10.]
- v0.9 — Gate 0a/0 ratified: audit frozen; numerics standards (§2.6); block accounting (D1); S4=θ3× (D2); E5 HiGHS arm dropped (D4); SSP axis; two-regime swing; corrected pass criterion; degeneracy prior→unknown.
- v0.8 — Universal transform screening; feature cards; E9 log-carbon arm.
- v0.7 — D3(b); multi-budget deferred; E6 INACTIVE; licensing sequencing.
- v0.6 — EFGs locked foundation; doubling-only tilts.
- v0.5 — PROACT blocks confirmed; S1=core-habitat-forward.
- v0.4 — No elicited weights; S0 analyst-constructed; frequency surface is the deliverable.
- v0.3 — R2 tail-mass criterion; constants frozen (θ=5×, λ=0.10, a_min=0.5%, t_min=0.15).
- v0.2 — §2.5 protocol; (w,t)-pair scenarios; E8–E10.
- v0.1 — Initial plan.

---

## 1. Purpose and contribution claims

Two-level hierarchical selection-frequency estimand for exact-solver conservation planning, published with an expressivity audit distinguishing genuine value-robustness from structural inability to express values — demonstrated on the Y2Y prioritization.

**Claim A (estimand, re-registered v0.10).** Per planning unit i: F_i = (1/|S|) Σ_s f_{i,s}, where f_{i,s} = i's membership fraction across k_s maximally-diverse solutions certified within formulation s's g-band (objective ≤ (1+g)·optimum). One vote per formulation; the vote is fractional. The within-cell object is interval-logical — membership breadth across the band's extremes — chosen over volume-weighted sampling deliberately (uniform vertex sampling intractable; plateau volume is geometry, not decision relevance). g is a certified hard wall and the lens parameter; the f(g) curve (E4) is the central robustness product: which cells survive every plan within 2% / 5% / 10% of optimal.

**Claim B (expressivity audit).** A frequency surface is interpretable only alongside per-feature influence decomposition and saturation status per cell. "Robust to values" and "structurally unable to express values" are observationally identical in F_i and mean opposite things. Now includes the amount-vs-places finding (M6.7): total-capture targets are silent about WHERE — a semantic dimension of target-based planning the audit surfaced and the scenario axis now expresses.

**Claim C (influence-space scenarios).** Scenarios as target influence profiles translated via the two-regime swing w·(min(cap_max,t)−cap_min)/t (linear-arm special case: w=influence/leverage). First-order caveat MEASURED at Gate 2: satiating member realizes 100% of claim, diffuse members ~40–50% of capacity under competition (m_soc realized 0.275 vs intended 0.186). Intended and realized reported side by side in T1; no iteration against realized swing (ruling, v0.10).

Applied payoff (Discussion): frequency surface feeds post-hoc corridor/compactness delineation. Out of scope (§14).

## 2. Constraints and measured facts

1. Influence follows the two-regime swing under all linear objective families; levers are weights, targets, feature definitions.
2. **Dominance (a0 control):** m_soc 54.2% capture (1.8× area share) with no carbon treatment — hygiene fixes (penalty removal, 1/v) made it WORSE than iter6's 45.5%. Motivation: hygiene does not substitute for influence accounting.
3. **Lever-selection principle (a4):** attainable target = claim magnitude; weight = priority below target. a4 reproduces a0 exactly (0/1.27M cells).
4. **Targets bind at the kink to four decimals** (a1 0.3320; a2 0.3000; a3 0.4000). Pass criterion: only BELOW target fails; co-capture overshoot expected.
5. **Amount ≠ places (M6.7, Gate 2):** m_soc target binds exactly yet its ≥5× tail captures only 0.435; biomass tail 0.425 at w=0.199 (was 1.000 at w=1). A total-capture target secures an amount, not places; only a constraint or feature defined on the places secures places. (Supersedes the refuted "weight sets depth, not order" claim — joint scoring lets competition invert within-feature hotspot preference.) Context: connectivity tail 0.980, refugia 1.000.
6. **Optimum uniqueness (M6.6, Gate 2):** S0's certified optimum is essentially unique — 50 k-best solutions within 3.2e-6 relative, pairwise Jaccard 0.9999, conditional cells 0.09%. PoolSearchMode=2's PoolGap acts as a bound, never a sampling target: the g-band was NEVER SAMPLED by k-best. Band diversity is unmeasured until Gate 2b. Both findings are paper material.
7. **Saturation is capacity, not outcome** (36/40 EFGs CAN saturate; which DO is per-cell; E7 verifies).
8. **Intactness R3-inexpressible** (0.042); published per D3(b) with mechanism and measured cost (0.074 vs 0.055; AOH–footprint +0.636/+0.607). Carried in the S0 objective at a nominal weight, influence disclosed in T1.
9. **w = t is retired** (Gate-0 diagnostic convention; S0 weights come from block accounting).
10. **Compute (measured):** certified single 55 s; k-best pool 1,799 s; HiGHS LP twin 6,516 s (worst presolve pathology — twins move to Gurobi LP path, HiGHS spot-check ×2 cells); MGA ≈ 55 s/solve ⇒ k=50 ≈ 50 min/cell. All portfolios Gurobi-gated; WLS 2 sessions ⇒ serial cells.
11. **S0 LP relaxation effectively exact:** 100.00% integral; LP-vs-MILP Jaccard 0.9957; max capture delta 0.0006.

## 2.5 Feature characterization protocol — EXECUTED; FROZEN (2026-08-26) + v0.10 masked-layer addendum

Constants: θ=5×, λ=0.10, a_min=0.5%, t_min=0.15 (frozen v0.3). Rules R1–R4 as amended v0.8.

| feature | leverage | θ-tail area | implied target | class → lever |
|---|---|---|---|---|
| carbon m_soc | 0.884 | 4.06% | 0.332 | concentrated-satiating → target |
| carbon biomass | 0.801 | 1.17% | 0.066 < t_min | diffuse-linear (REVERTED by rule) → weight |
| transboundary connectivity | 0.461 | 0.20% < a_min | — | diffuse-linear → weight (pinch-point spike, not a tail) |
| climate macrorefugia (1/v) | 0.422 | 0.47% (fails a_min, t_min) | — | diffuse-linear → weight |
| climate corridors | 0.263 | no crossing | — | diffuse-linear → weight |
| AOH birds / mammals | 0.232 / 0.181 | no crossing | — | diffuse-linear → weight |
| gHM intactness | 0.042 | — | — | R3 inexpressible (disclosed) |
| EFG block (40) | — | — | — | 36 rare-attainable + 4 unsaturated disclosed |

Transform screening: log1p flips m_soc to diffuse-linear (0.884→0.486; E9 screening evidence) and pushes AOH birds under the floor (0.232→0.059). Only the standing 1/v orientation adopted. Cards: 10 pages, archived.

**Tail-mask contingency — RESCINDED (v0.12, M4.14):** no masked tail features under any standing authorization; the S4 pilot passed on the existing stack (§3.1), and any hypothetical future tail-capture failure returns to design discussion rather than triggering an escalation. The once-executed v0.10 machinery is archived and its quarantined layers retained as verified backup knowledge (both tails rare-attainable; exact frozen-T2 matches). The feature stack is the original eight + EFGs, unchanged, in every cell.

## 2.6 Numerics and solver standards (ratified v0.9; extended v0.10)

1. False-certificate vignette (supplementary): Gurobi certified a wrong optimum twice, bit-identically (root LP mis-converged 0.42% high; matrix range [1e-11,1e5] from resampling dust). Caught by the integral LP twin.
2. Dust thresholding: <1e-9 of feature total → 0 (biomass 46,097 cells / 0.0015% mass; m_soc 19,262; connectivity 538). Range now [1e-4,1e5]. Audit invariant. Per-feature drop disclosure.
3. Standards: opt_gap 1e-4 (single solves); NumericFocus 2 (also 60× faster on a4); **LP twin beside every certified MILP** — v0.10: twins PRODUCED on Gurobi's LP path for the ensemble, HiGHS re-verification on 2 cells as spot-check; open-verification claim = twins verifiable without Gurobi, not produced without it.
4. Provenance: per-solution solver record (objective/status/gap/runtime) in run_summary; engine-vs-recomputation reconciliation ≤3e-5 abs. Shims disclosed (Gurobi-13 `xn`→`poolnx`; portfolio list-return).
5. MGA machinery (v0.10): compiled-model extraction adds the band constraint row (objective ≤ (1+g)·certified optimum) and swaps the objective to max Hamming distance from incumbents (discretionary cells); every member carries a per-solution band certificate.

## 3. Formulation space

### 3.1 Value scenarios (block-budgeted; amount-vs-places axis added v0.10)

Every scenario = complete (w, t) pair over ONE feature stack (incl. masked tail features at t=0 unless activated). Levers follow frozen classes.

**S0 — Balanced (AMOUNT semantics by decision, v0.10; formulation unchanged from Gate 2).** Equal discretionary influence per block via joint block accounting in two-regime swing currency; carbon block splits mass-proportional 74.2/25.8 (stands on the S0 amount decision). Frozen weights (scenarios_v1.json): refugia 1.460 / conn 0.669 / corridors 1.171 / m_soc 0.465 @ t=0.332 / biomass 0.199 / birds 1.329 / mammals 1.708; intactness nominal, disclosed. Measured (certified single): captures refugia 45.2 / conn 33.4 / corridors 26.0 / m_soc 33.2 (AT target) / biomass 31.0 (in the 30–37 pre-stated band — "co-benefit, not driver" achieved) / birds 33.0 / mammals 32.7 / intactness 29.9. θ-tail capture: m_soc 0.435, biomass 0.425 — the pre-stated biomass expectation FAILED and is REPORTED (v0.9.1 commitment): the honest signature of amount semantics. The +8.4-pt w=t biomass leak remains the motivation for block accounting.

| ID | Scenario | Lever(s) | Definition |
|---|---|---|---|
| S0 | Balanced | (w, t) as frozen | Amount semantics; tails ride co-capture only; tail features t=0 |
| S1 | Core-habitat-forward | Weights | Core habitat block share doubled; others scaled down |
| S2 | Connectivity-forward | Weights | Connectivity block share doubled. Carroll static-layer disclosure |
| S3 | Biodiversity-forward | Weights | Biodiversity block share doubled; mandatory gHM audit |
| S4 | Carbon-forward (amount + places pressure — PILOT PASS v0.12) | Target + weights on the EXISTING stack | Amount dial: θ 5×→3× ⇒ m_soc t=0.552. Places pressure: block-doubled carbon weights. **Measured (iter10, certified 54 s):** m_soc tail 0.960, biomass tail 0.772 (band ≥0.75 PASS); totals 0.552 (at target) / 0.329; biomass total +1.9 pts while tail nearly doubled — pressure REDIRECTS dense-first. Zero formulation change; full stack symmetry across 14 cells. F9 panel: S0 0.435/0.425 → S4 0.960/0.772. T1 reports REALIZED block-influence ratio. a0 (54.2%) cited as the unconstrained bracket |
| S5 | Intactness-forward | R3-inexpressible per D3(b) | Published as expressivity finding; footprint-as-cost = future work |
| S1.5/S2.5 (optional) | Midpoints | Weights | Only if corners diverge cliff-like |

Each scenario ships: intended profile; (w, t) pair; realized captures; realized influence (two-regime swing; intended-vs-realized side by side, no iteration); per-cell saturation outcomes (only below-target fails); **θ-tail capture per feature with scenario-specific pre-stated expectations**; gHM audit (mandatory S3).

### 3.2 Other axes

| Axis | Levels | Crossing |
|---|---|---|
| Climate (emissions) | SSP245 vs SSP585, both 2071–2100; macrorefugia 1/v per realization (no anchor parameter; leverage 0.422–0.516; top-30% Jaccard 0.574). Constant-influence-per-scenario: intended profiles held fixed across climate levels; weight vectors re-derived per cell from layer-specific swing | Full cross |
| Carbon regime | S0-level (θ=5×, amount) vs S4-level (θ=3× + tail locks, places) | S4 + crossed subset {S1,S3}×carbon-forward for E3 |
| Budget | 30% fixed | Multi-budget deferred |
| Gaps | opt_gap 1e-4 (certificate); pool/band gap g (estimand lens) | g fixed for main results; E4 varies g |

Disclosure: climate corridors static (Carroll 2018); the axis propagates climate uncertainty through macrorefugia only. Cell count: 14 (6 scenarios × 2 climate + 2 crossed). Cost ≈ 14 × (55 s anchor + ~50 min MGA + one k-best pool), serial.

## 4. Estimand and aggregation (RE-REGISTERED v0.10)

- Within-cell: generate k maximally-diverse members of the g-band by iterative max-Hamming MGA against the certified anchor; f_{i,s} = membership fraction. Semantics: breadth across the band's extremes (robust core = f=1; never-set = f=0; exchangeable middle), NOT volume-weighted probability. Per-solution band certificates recorded.
- Cross-cell: F_i = mean over cells, one vote per cell; never flat-pool. Fewer-than-k members: use what exists.
- g and k reported; claims phrased "within the g-band." **f(g) at g∈{2%,5%,10%} is the central robustness product** (core erosion with tolerance). k sufficiency = diameter and core-share stabilization on the reference cell.
- **Degeneracy verdict — MEASURED (Gate 2b, rule v2 frozen pre-run, hash v2_8db80fed1c702638): PLATEAU-RICH.** D = 0.953 (achieved Hamming diameter, 95% of the two-disjoint-selections ceiling), C = 0.020 (f=1 core share) at g=5% — thresholds (D≥0.10, C≤0.90) passed by an order of magnitude. Claim A carries. f(g) core erosion: 12.0% of selection at g=2% → 2.0% at 5% → ZERO at 10% (union ≈ every discretionary cell). Logical status: zero f=1 among 50 certified band members PROVES no universally-required cell; f=1 cores at tighter g are upper bounds on the truly-required set — captions phrase as "in every generated plan." Two-instrument geometry (with M6.6): sharp essentially-unique optimum on a wide shallow plateau — near-disjoint 30% plans exist within 5% of optimal.
- Bands (presentational): always ≥0.95 / frequent 0.70–0.95 / conditional 0.30–0.70 / rare 0.05–0.30 / never <0.05. Analyses use continuous F. Overlap coefficient between cells.

## 5. Expressivity audit (E7)

Per cell s, per feature f: two-regime swing influence, w_f, t_f, saturation OUTCOME, realized capture ratio, θ-tail capture rate (scenario-specific expectations: S0 biomass ~0.43 reported-as-failed; S4 acceptance band ≥0.75 both pools, or escalation record). Products: live-feature map of the design; robust-vs-inexpressible figure (Claim B); amount-vs-places panel (M6.7 → S0/S4 contrast).

## 6. Experiments

| ID | Comparison | Supports | Status / criterion |
|---|---|---|---|
| E1 | Hierarchical F vs one-solve-per-cell | Claim A | Under the MGA estimand; effect-size prior unknown; Gate 2b decisive |
| E2 | Hierarchical vs flat pooling | Claim A semantics | Divergence where cells return unequal k |
| E3 | Variance decomposition: scenario / climate / carbon-regime / within-cell | Attribution | Includes the amount-vs-places regime as an axis level |
| E4 | f(g) at g∈{2,5,10%} × k-sufficiency | Central robustness curve | MEASURED on reference cell (core 12.0%→2.0%→0); ensemble runs g=5% only; F6 ready |
| E5 (DISCHARGED — measured) | k-best vs MGA | Enumeration-order bias | Two-instrument geometry: 50 near-clones (3.2e-6) vs near-disjoint plans (5%) — the contrast is the thesis figure |
| E6 (INACTIVE) | Marxan | — | Deferred; time-boxed if activated |
| E7 | Expressivity audit | Claim B | §5 |
| E8 | Carbon-weight ×10 inertness at S0 + most-hostile cell | Lever principle | a4 proves it at Gate-0 scale; confirm under S0/S4 weights |
| E9 (screening in hand) | S0-protocol vs influence-weights-100% vs log-carbon-100% | Target lever justification | log1p class-flip = screening result; solves confirm per-hectare predictions |
| E10 (partially discharged) | θ∈{3,5,10×} | R4 defensibility | Targets are archive lookups; capture-shift solves remain |
| E11 (NEW v0.12.1) | Two-level solution-space spread: between-anchor distance matrix vs within-cell diameters (matched currency) + cross-objective suboptimality Δ(s,s′) = (obj_{s′}(x_s) − obj*_{s′})/obj*_{s′} | Separates value disagreement from near-optimal freedom | MEASURED (R8.6): within-diameters exceed between-anchor distances; 156/182 mutually in-band; 26 exceptions all carbon-forward objectives (Δ 0.085–0.093) |
| E12 (v0.13) | Bracketing estimator: random-objective-direction band sampling (MAA/SPORES lineage) vs MGA, k=50, same band | Bounds estimator-conditionality of f, bands, and E3 shares | 3 formulations (S0-ref, S2, S4), then decide full 14; report both estimators as the bracket; ~45 min/formulation |
| E13 (v0.13) | Frequency attribution: f>0.7 overlays vs m_soc tail / rare-EFG / connectivity-spike masks; f-by-value-decile curves; S2 manifest sanity | Q2 mechanism (binding scarcity vs substitutable emphasis); Claim B spatial form | Zero solves. Confirms or kills the "carbon cluster out-connects the connectivity cluster" explanation before it enters Discussion |
| E14 (v0.13) | Per-feature capture spread across each formulation's 51 members | Is the aggregate band a de-facto per-value band? | Zero solves from stored representations; decides E15 |
| E15 (conditional on E14) | Guardrailed-band MGA: + rows capture_b ≥ (1−g)·anchor_b per block | Secondary "no value left behind" semantics | 2–3 formulations; pre-registered as secondary; aggregate band remains primary estimand |
| E16 (v0.13, compute-gated) | Band-constrained delineation: min boundary s.t. band (+ E15 floors) | Contiguity from plateau slack, not penalty; applied deliverable | 1–2 formulations; fallback = F-guided post-hoc clustering |
| E17 (v0.13.1) — Latitudinal attribution | T1 (zero-solve): latitudinal selection profiles (share of discretionary selection vs available area per latitude band) for every anchor + F, benchmarked against the locked-PA baseline (whose own southern-parks/rock-and-ice bias must not be attributed to the optimizer); per-feature latitudinal mass curves (supply side of pull); E13 overlays re-cut by latitude. T2 (zero-solve): rare-EFG footprint geography (36 locked + 4 unsaturated) — if predominantly southern, a representativeness-adequacy disclosure regardless of other results. T3 (~5 solves, ~1 min each): leave-one-BLOCK-out counterfactual anchors at S0 (drop biodiversity / EFG foundation / carbon / core habitat / connectivity in turn), latitudinal profile shift per removal | Answers "is the formulation leaning somewhere nobody chose" causally, and IS the drop experiment run as diagnostic (Kujala leave-one-out lineage, block level) — blocks are NOT dropped from the formulation (PROACT-selected values; deletion redistributes influence = a different value position, and post-results value surgery is the reviewer red flag the manifest exists to prevent). Pre-stated expectations: biodiversity-out shifts north; carbon-out shifts south; EFG-out unknown (that is the point). If a problematic lean confirms: remedy ladder = disclosure + scenario reading → applied-paper layer replacement (rarity-weighted richness / species-level AOH — addresses the measured AOH indictments: footprint correlation, small-body dominance, validation asymmetry, latitudinal mass) → formulation change last | Third face of the audit discipline: influence accounting (leverage), semantic accounting (amount-vs-places), spatial-equity accounting (latitude) — what did the formulation decide that nobody chose |

## 7. Gates

**0a — COMPLETE** (audit frozen 2026-08-26; cards archived). **0 — COMPLETE, ALL PASS** (2026-08-27; binding to 4 decimals; a4 exact reproduction; freed-budget map incl. corridors −1.4 mis-prediction reported; LP tightness measured).
**1 — COMPLETE as frozen** (scenarios_v1.json; S0 solved and certified; T1 measured; tail diagnostic fired and reported).
**2 — EXECUTED; verdict superseded by instrument finding** (M6.6). Artifacts stand as the measured record (anchor optimum, pool-cost, uniqueness result, E4 seed).
**2a — COMPLETE** (scenarios_v2.json; S4 pilot PASS 0.960/0.772; contingency rescinded M4.14; MGA loop built; verdict rule v2 frozen pre-run).
**2b — COMPLETE, PLATEAU-RICH** (anchor exact 8.1 s; 150/150 members over g∈{2,5,10%}, zero duplicates, all band certificates binding exactly; D=0.953, C=0.020; f(g) curve measured; E4 seeded).
**3 — AUTHORIZED (v0.12): freeze NOW.** Manifest per §9 with the measured parameters (14 cells; estimator mga_maxham_v1, k=50, g=5%; MIPGap_dist 0.01; TimeLimit_iter 900; opt_gap 1e-4; NumericFocus 2; verdict_rule v2_8db80fed1c702638; per-cell artifact set anchor/mga/kbest/Gurobi-path twin; layer sha256s) + pre-registration freeze. Freeze confirmations: climate cells hold intended influence constant with per-cell weight re-derivation (§3.2); E4 extra g-levels reference-cell-only.
**4 — COMPLETE (2026-09-01, R8):** 13 formulations serial, wall ≈22 h; anchors exact 44–58 s; twins pass; MGA 36–50 min; pools 549–1,822 s (s3_ssp585 38/50 at limit, disclosed). F_surface.tif delivered; E1–E4, E7, E11 measured.
**4b (NEW v0.13) — Post-R8 analysis round:** E13+E14 (zero-solve) immediately; E12 on 3 formulations; E15 conditional; E16 demo; E8–E10 supplementary solves pre-draft.
**5 — ENTERED (parallel):** write-up architecture (Claim A spine = two-instrument geometry + empty always-band + E1; Claim B = E7/E13; Claim C = E11/no-regrets; numerics vignette supplementary). BLOCKING before novelty framing: García-Quintas full text (email out this week); Lehtomäki & Moilanen skim; fresh sweep at submission.
**5 — Write-up.** BLOCKING: García-Quintas et al. 2025 full text; Lehtomäki & Moilanen 2013 skim; fresh sweep at submission. Results inventory now includes M6.6 (pool mechanism) and M6.7 (amount≠places).

## 8. Compute plan

Serial cells on Gurobi WLS (2 sessions). Per cell: anchor 55 s + MGA k=50 ≈ 50 min + k-best pool ≈ 30 min + Gurobi-path LP twin. Ensemble ≈ 1.5–2 h/cell ⇒ ~1–1.5 days wall. E4's extra g-levels on the reference cell only. HiGHS spot-check twins ×2 cells budgeted separately (~2 h each, worst case 109 min observed).

## 9. Manifest schema

Per cell: `cell_id`, `scenario_id`, `scenario_name`, `climate_level` (ssp245_2071_2100 | ssp585_2071_2100), `carbon_regime` (theta5_amount | theta3_places), `budget_pct`(=30), `weight_vector` (JSON), `target_vector` (JSON incl. tail features), `influence_profile_intended` (JSON, two-regime swing), `k_requested`, `band_gap_g`, `opt_gap`(=1e-4), `numeric_focus`(=2), `dust_rule_version`, `estimator`(=mga_maxham_v1), `verdict_rule`(=v2_frozen_hash), `solver`, `seed_policy`, `input_layer_hashes` (JSON), `created_utc`, `frozen`. Companion: `manifest_freeze.sha256`. Post-freeze changes ⇒ new manifest version + changelog.

## 10. Directory / file contract

```
project_root/
  spec/frequency_ensemble_study_plan.md
  spec/manifest.csv + manifest_freeze.sha256
  spec/scenarios_v2.json                      # v0.10 re-freeze (S4 + tail features; S0 unchanged)
  audit/feature_cards/                        # 10 pages + masked-layer addendum cards
  audit/audit_objects/                        # budget-independent npz (any-θ/any-B lookups)
  analyses/y2y/01..04 + gate2 notebooks       # executed record
  runs/<cell_id>/anchor/ mga/ kbest/ twin/    # per-cell artifact set (v0.10 layout)
  runs/gate2_s0_ref/                          # superseded-but-kept measured record
  analysis/e1_bias/ ... analysis/e10_theta/
  figures/
  changelog.md  results_log.md  methods_log.md
```

## 11. Figures and tables plan

F1 estimand schematic (two-level + band wall). F2 E1 bias map. F3 frequency surface + always-core. F4 attribution small-multiples. F5 expressivity figure (Claim B). **F6 f(g) core-erosion curves (E4 — central).** F7 k-best-vs-MGA contrast (E5 by-product; M6.6 panel). F8 marginal-density trajectories with θ crossings. **F9 amount-vs-places panel (S0 vs S4 tail capture; M6.7).** **F10 (v0.12.1) two-level spread: embedding of the 14 anchors in selection-distance space, each carrying its within-cell diameter disk, + Δ(s,s′) cross-objective heatmap — overlapping disks with small Δ = pluralism within tolerance; separated disks with large Δ = measured value conflict.** T1 scenarios (intended → (w,t) → realized captures + realized influence side-by-side + realized block ratios + tail rates). T2 frozen characterization (+masked addendum). T3 expressivity summary incl. mission-selected-vs-implementable gap. Suppl.: numerics vignette (false certificate, dust, LP-twin methodology, 60× result, HiGHS pathology), gHM audits, E8–E10, manifest, cards. Maps ESRI:102008; CVD-checked ramps.

## 12. Positioning and citations (verify DOIs before tracker entry)

Formulation lineage: Jung et al. 2021 NEE (10.1038/s41559-021-01528-7); foil — Jung's "equal" carbon weight = cardinality normalization, blind to spatial structure. **Currie, Liang & Snider 2025 CSP 7:e70087 (10.1111/csp2.70087) — two positioning roles (v0.14.2):** (i) closest Canadian precedent for Claim A's OUTER level — selection frequency summed across a 4-level target sweep (20–50%), one prioritizr/Gurobi solve per level at 100 km² hexbins, no within-formulation sampling; its summing across NESTED target levels is the convention the budget-stratification argument corrects; (ii) second Claim-C foil alongside Jung — species targets weighted 1/742 "to ensure that all Target 3 indicators equally contributed" = cardinality normalization; two independent national teams reaching for count-based equal contribution evidences the influence problem as real and unsolved. Also the qualitative precedent for E17 (species-only vs non-species runs attribute the north–south split, their Fig. S10) — cite as the finding this study decomposes causally. Scenario-ensemble lineage: Buenafe et al. 2023 (10.1002/eap.2852); Brito-Morales et al. 2022; Chapman et al. 2025 (10.1038/s41559-025-02671-1); Liczner et al. 2023 (10.1111/csp2.12994); Carpenter-Kling et al. 2025. Aggregate-solutions-not-inputs: Meller et al. 2014. Univariate-characteristics anchor: Kujala, Moilanen & Gordon 2018 MEE (10.1111/2041-210X.12939) — post-hoc/solve-based; §2.5 inverts to ex-ante/zero-solve (leverage↔μ* ρ=0.922). Partial precedents: Rodrigues et al. 2004 (rarity-scaled targets); Moilanen 2007 / Arponen et al. 2005 (benefit-function shapes chosen a priori); Marxan Good Practices (QA). MGA lineage now load-bearing for the estimator: Brill 1979; Brill, Chang & Hopkins 1982; Brunel et al. 2023 (10.1007/s10666-022-09862-1) — cite as the direct method parent. Highest prior-art risk: García-Quintas et al. 2025 (10.1016/j.biocon.2025.111447) — Gate 5 blocking. Novelty sentence unchanged; add the amount-vs-places finding as a second, independent contribution sentence for the targets literature. Gate 5 skim: Lehtomäki & Moilanen 2013.

## 13. Decisions

All resolved or deferred. **v0.13 rulings:** boundary penalty STAYS OUT of the objective (unelicited value with uncalibrated influence share; 400× solve cost; post-results re-freeze) — contiguity via E16's band-constrained delineation or post-hoc clustering; tilt magnitudes UNCHANGED (dispersion is band width not anchor position; f pins via binding scarcity not weight; sanctioned dials for candidate-cluster drawing = g=2% per-formulation surfaces + the E12 bracket reading); aggregate band = PRIMARY estimand (faithful to elicited trade-offs), guardrailed per-block band = optional secondary semantics via E14→E15. **v0.12:** S4 pilot PASS; tail-mask contingency RESCINDED (M4.14). **v0.11:** carbon-forward = (w,t) on existing stack. **v0.10:** D-A(a) MGA estimator; D-B amount-vs-places axis. **Rulings:** no iterate-once; Gurobi-path twins + HiGHS spot-check ×2. Standing: D3(b); EFGs locked; doubling-only; constants frozen; mass-proportional carbon split. Deferred: multi-budget; Marxan E6.

## 14. Out of scope (fenced)

Post-hoc delineation (Discussion only). PROACT elicitation methodology. Eastern-slopes grizzly analysis. Budget stratification beyond the deferred note. Sobol'. Applied-paper full PROACT treatment.
